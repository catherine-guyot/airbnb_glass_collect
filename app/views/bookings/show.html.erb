<div class="container " >

  <h1> Information on your reservation </h1>

  <p>Thank you for your reservation from <%=@booking.start_date %> to <%= @booking.end_date %></p>
  <p>You will be welcomed <%= @booking.flat.address %>, in <%= @booking.flat.city %>.

  <% duration = @booking.end_date  - @booking.start_date  %>
  You will stay <%= duration.to_i %> days
  <p>le prix par nuit de votre appartement est de : <%= @booking.flat.price_per_night %>€ </p>
  <p> Le montant total de votre réservation sélève à <%= duration.to_i * @booking.flat.price_per_night %>€</p>
  Le status de votre réservation est actuellement de "<strong><%= @booking.status %>"</strong>.
  </p>
  <% if current_user == @booking.user %>
        <%= button_to 'Cancel the reservation', flat_booking_path(@booking.flat, @booking.id), method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-danger mt-2%'%>
  <% end %>
</div>
