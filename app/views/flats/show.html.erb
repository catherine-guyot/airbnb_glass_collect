<div class="container">
  <h2>Bienvenue</h2>
  <div class="flat-card">
    <div class="flat-card-content">
      <p><%= @flat.address %>, <%= @flat.city %></p>
      <%= cl_image_tag @flat.photo.key, alt:"flat_picture", height:300, width:400, crop: :fill, class: "rounded-image" if @flat.photo.attached? %>
      <p><%= @flat.description %></p>
      <div class="flex">
        <div class="flex align-items-center">
          <i class="fa-solid fa-people-line"></i>
          <span><%= @flat.number_of_guests %> personnes</span>
        </div>
        <div class="flex align-items-center">
          <i class="fa-solid fa-euro-sign"></i>
          <span><%= @flat.price_per_night %> par nuit</span>
        </div>
      </div>
    </div>
    <div class="flex align-items-center">
      <%= link_to 'Réservez!', new_flat_booking_path(@flat), class: "btn btn-outline-success mt-2" %>
      <% if current_user == @flat.owner %>
      <%= button_to 'Delete', delete_flat_path(@flat), method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-danger mt-2'%>
      <% end %>
    </div>
  </div>
</div>
